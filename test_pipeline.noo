# Types flow through pipelines
result = [1, 2, 3]
  | map (fn x => x * 2)      
  | map show                 
  | head;                    

# Function composition preserves types
double = fn x => x * 2;
increment = fn x => x + 1;
composed = double |> increment;
result
