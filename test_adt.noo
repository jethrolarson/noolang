# Test current ADT and generic support

# Basic ADT
type Option a = Some a | None;

# Generic function
identity = fn x => x;

# ADT construction and pattern matching
opt = Some 42;
result = match opt with (
  Some x => x;
  None => 0
);

result