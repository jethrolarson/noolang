# Test ADT with higher-order functions
type Option a = Some a | None;

# Higher-order function working on Option
option_map = fn f opt => match opt with (
  Some x => Some (f x);
  None => None  
);

increment = fn x => x + 1;
my_option = Some 5;

option_map increment my_option