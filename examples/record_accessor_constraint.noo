# Record Accessor Constraint Demo

# Define a record with a @name field
person = { @name "Alice", @age 30 };

# Use the @name accessor
get_name = @name;

# Apply the accessor to the record
person_name = get_name person;  # Should be String

# Try to use the accessor on a record without the field (should fail type checking)
# not_a_person = { @age 42 };
# bad = get_name not_a_person;  # Should fail

# Final result
person_name 