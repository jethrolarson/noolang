# Import the standard library
stdlib = import "std/std";

# Use math functions directly
result1 = 5 + 3;

# Use list functions
list = [1, 2, 3, 4, 5];
first = head list;

# Use string functions
greeting = concat "Hello, " "Noolang!";

# Create a record with results
{ @math_result result1,
  @list_result first,
  @string_result greeting }