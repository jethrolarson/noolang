# Import math module directly
math = import "std/math";

# Use math functions with correct accessor syntax
add = @add;
multiply = @multiply;
square = @square;

result = add (multiply math 2 3) (square math 4);

# Return the result
result 