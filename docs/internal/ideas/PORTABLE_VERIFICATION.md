Is there a way to communicate verification across agent boundaries? Like say one agent ran a test can that verification be shared between agents? Like can the test result cryptographically confirm that specific compiler+source ran and produced a result? Then a coding agent can run a test and then a verifier agent can see that the code was already verified and skip rerunning expensive processes provided the provenance is sound. I think there was a language called "unison" with this idea many?
